spring.datasource.url=jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${DB_DB:nse}
spring.datasource.username=${DB_USERNAME:postgres}
spring.datasource.password=${DB_PWD:password}


loader.ticker.scheduler.cron="0 5 */1 * * *"
loader.dsp_ticker.scheduler.cron="0 10 16,17,18,19,20,21,22,23 * * *"
loader.bulk_ticker.scheduler.cron="10 */4 * * * *"